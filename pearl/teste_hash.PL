 my %hash = ("a" => 1, "b" => 2, "c" => 3);
# my @Lista_chaves = keys%hash;
# my @lista_valores = values%hash;
  while (($key, $value) = each %hash ){
  print "$key => $value\n";
  echo date;
  }